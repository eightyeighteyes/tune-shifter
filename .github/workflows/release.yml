name: Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Poetry
        run: pip install poetry

      - name: Build sdist
        run: poetry build --format sdist

      - name: Locate tarball
        id: tarball
        run: |
          TARBALL=$(ls dist/*.tar.gz | head -n1)
          echo "path=${TARBALL}" >> "$GITHUB_OUTPUT"
          echo "name=$(basename ${TARBALL})" >> "$GITHUB_OUTPUT"

      - name: Compute SHA256
        id: sha256
        run: |
          SHA=$(shasum -a 256 "${{ steps.tarball.outputs.path }}" | awk '{print $1}')
          echo "value=${SHA}" >> "$GITHUB_OUTPUT"

      - name: Upload sdist to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" \
            "${{ steps.tarball.outputs.path }}"

      - name: Update Homebrew tap
        env:
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          TAG: ${{ github.event.release.tag_name }}
          SHA256: ${{ steps.sha256.outputs.value }}
          TARBALL_NAME: ${{ steps.tarball.outputs.name }}
          GH_USERNAME: eightyeighteyes
        run: |
          VERSION="${TAG#v}"
          URL="https://github.com/${GH_USERNAME}/tune-shifter/releases/download/${TAG}/${TARBALL_NAME}"
          git clone "https://x-access-token:${HOMEBREW_TAP_TOKEN}@github.com/${GH_USERNAME}/homebrew-tune-shifter.git" tap
          cp Formula/tune-shifter.rb tap/Formula/tune-shifter.rb
          cd tap
          sed -i "s|url \".*\"|url \"${URL}\"|" Formula/tune-shifter.rb
          sed -i "s|sha256 \".*\"|sha256 \"${SHA256}\"|" Formula/tune-shifter.rb
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add Formula/tune-shifter.rb
          git commit -m "tune-shifter ${VERSION}"
          git push
